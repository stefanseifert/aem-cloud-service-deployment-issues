{
  "labels": [
    "Static Value",
    "env:server-url",
    "env:my-env-param",
    "secret:server-api-key",
    "secret:customer-secrets/server-api-key"
  ],
  "values": [
    "Value #1",
    "$[env:server-url]",
    "$[env:my-env-param]",
    "$[secret:server-api-key]",
    "$[secret:customer-secrets/server-api-key]"
  ]
}
